# Pixel Decomposer V0.1.6

## 中文

- 黑白浅色、深色、跟随系统和定时日夜主题；设置中可调整文字大小和玻璃效果。
- 绘画工具移入占位工具区，添加图标及文字，优化手机横竖屏布局。
- 每帧独立图层：新建、导入、复制、删除、排序、命名、隐藏、锁定、透明度及向下合并；工程保留图层，支持撤销和重做。
- 新增圆头画笔、柔边画笔、圆头橡皮、文字和笔压输入；保留像素绘画、形状、选区及镜像工具。
- 多帧复制、排序、逐帧时长、播放和洋葱皮预览。
- 无需拆帧即可导出整图透明 PNG，或将整图打包为 ZIP。

本地验证包括图层合成与透明度、撤销、复制帧、工程恢复、圆头绘画、鼠标/触摸缩放、整图导出和桌面/手机视口布局。手机视口验证不是安卓真机测试。发布附件为 Windows 安装程序和安卓 APK。

## English

- Monochrome light/dark, system and scheduled themes, adjustable text size and optional glass effects.
- Drawing tools occupy their own layout space with icons and labels, including phone portrait/landscape layouts.
- Per-frame layers support creation, import, duplication, deletion, reordering, naming, visibility, locking, opacity and merge down. Layers persist in projects and support undo/redo.
- Round and soft brushes, round eraser, text and pen pressure supplement pixel tools, shapes, selections and symmetry.
- Frame duplication, reordering, duration, playback and onion-skin preview.
- Export a whole transparent PNG or ZIP without splitting first.

Local checks cover compositing, opacity, undo, frame duplication, project restoration, round-brush painting, mouse/touch zoom, whole-image export and responsive viewports. Viewport simulation is not Android device testing. Release assets: Windows installer and Android APK.

## 日本語

- モノクロのライト／ダーク、システム連動、時間帯連動テーマ。文字サイズとガラス効果を設定できます。
- 描画ツールを専用領域に配置し、アイコンと説明を併記。スマートフォンの縦画面・横画面を調整しました。
- フレームごとのレイヤー作成、読み込み、複製、削除、並べ替え、名前変更、表示切替、ロック、不透明度、下のレイヤーとの結合。プロジェクト保存と元に戻す／やり直しに対応。
- 丸ブラシ、ソフトブラシ、丸消しゴム、文字、筆圧を追加。ピクセル描画、図形、選択範囲、対称描画も利用できます。
- フレームの複製、順序変更、表示時間、再生、オニオンスキン。
- 分割せずに画像全体を透過 PNG または ZIP で書き出せます。

ローカル検証では合成、不透明度、取り消し、フレーム複製、プロジェクト復元、丸ブラシ、ズーム、画像全体の書き出し、画面サイズ別の配置を確認しました。画面サイズのシミュレーションは実機テストではありません。配布ファイルは Windows インストーラーと Android APK です。
